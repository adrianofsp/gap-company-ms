name: Build and Test

on:
  push:
    branches: [develop]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/chekout@v2

      - name: Runs Tests
        run: ./gradew build